#include "main.h"
/**
 * jack_bauer - print every minute
 *
 * Return: no return
 */
void jack_bauer(void)
{
	int w, x, y, z;

	for (w = 48; w <= 50; w++)
	{
		for (x = 48; x <= 50; x++)
		{
			for (y = 48; y <= 50; y++)
			{
				for (z = 48; z <= 50; z++)
				{
					if (a >= 50 && b >= 52)
					break;
				_putchar(w);
				_putchar(x);
				_putchar(58);
				_putchar(y);
				_putchar(z);
				_putchar('\n');
			}
		}
	}
}
