#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *
 * Description: compute integer values
 * @n: input unmber as integer
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (n * -1);
	}
}
